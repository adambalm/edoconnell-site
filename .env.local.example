# Sanity — project defaults are hardcoded in config, but can be overridden here
PUBLIC_SANITY_PROJECT_ID=your-project-id
PUBLIC_SANITY_DATASET=production

# Sanity API token — required for draft fetching and visual editing
# Create at: https://manage.sanity.io > Project > API > Tokens (Viewer role minimum)
SANITY_API_READ_TOKEN=

# Write token — required for seed/patch scripts only
# SANITY_API_WRITE_TOKEN=

# Visual editing — set to "true" to enable stega encoding and edit overlays
PUBLIC_SANITY_VISUAL_EDITING_ENABLED=false
